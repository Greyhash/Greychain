import os








class Item:

    def __init__(self, sender, item_type, receiver, time, message, comment)
        self.sender = sender
        self.item_type = item_type
        self.receiver = receiver
        self.time = time
        self.message = message
        self.comment
        
        
        def item_type(self, type_of_item)
            return type_of_item
            
        
        def owners(self, owner_id)
        

     
